https://www.jianshu.com/p/2acbce31780d
在修改前，先git pull origin dev:dev使本地分支与远程同步，不行的话强制同步 git pull -f origin dev:dev ,然后把修改的代码粘贴过来。立马提交并git push origin dev:dev

git init
git status
git add filename
git checkout filename
git commit filename -m message
git reset head filename
git log -n
ssh-keygen -t rsa -C "382669441@qq.com"
git remote add origin git@github.com:209914097/HWsh
ssh -T git@github.com
git remote rm origin解决fatal: remote origin already exists.
git remote add origin git@github.com:209914097/Git
提交全部 git commit -a 
git stash
git stash pop
git rm -r --cached . 解决.gitignore失效问题
git tag
git tag v1.0
git show v1.0
git tag -d v1.0
git checkout -b dev origin/dev远程先开好分支然后拉到本地
本地先开好分支然后推送到远程
git checkout -b dev
git push origin dev:dev #推送本地分支(冒号前面的)到远程origin的dev分支(冒号后面的)(没有会自动创建)
git pull <远程主机名> <远程分支名>:<本地分支名>

版本回退git reset --hard HEAD^
撤销工作区修改(在add之前)git checkout -- readme.txt

把远程分支拉到本地
git fetch origin dev（dev为远程仓库的分支名）
git checkout -b dev(本地分支名称) origin/dev(远程分支名称)
git pull origin dev(远程分支名称)

然后使用 git push 会把代码推送到远程分支dev上。
相当于 gir push origin dev:dev
